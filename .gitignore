# Jekyll
Gemfile.lock
_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/

# Python
__pycache__/
*.pyc

# OS
.DS_Store
._*

# release intermediates (the .tsq is regenerated each run; .ots.bak is the ots-upgrade backup)
*.tsq
*.ots.bak

# scaffolder outputs (one-off, meant to be hand-merged into corpus.jsonl)
entry-*.jsonl
